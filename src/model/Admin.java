package model;
public class Admin extends User {
    public Admin(String id, String name, String lastname, String email, String password) {
        super(id, name, lastname, email, password);
    }
}
